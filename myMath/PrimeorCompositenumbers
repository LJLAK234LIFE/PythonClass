# I am choosing a random number
num = 407

# We ahve to take imput from the user
# then num = int(input("Enter a number: "))

# prime numbers are always greater than 1
if num > 1:
    # check the factors
    for i in range(2,num):
        if (num % i) == 0:
            print(num,"is not a prime number")
            print(i,"times",num//i,"is",num)
            break
    else:
      print(num,"is a prime number")

# if the input is less than or equal to 1, it is not a prime number
else:
    print(num,"is not a prime number")
